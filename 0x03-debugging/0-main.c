#include "main.h"
#include <stdlib.h>
#include <time.h>
/**
 *main-program entry point.
 *Return:0-no error,non zero value if error.
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
